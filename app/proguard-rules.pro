# Add project specific ProGuard rules here.
# GeckoView ships its own consumer ProGuard rules bundled in the AAR, so no
# extra keep rules are required here for a debug build (minifyEnabled = false).
